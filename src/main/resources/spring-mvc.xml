<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Включение аннотаций Spring MVC (@RequestMapping, @GetMapping и т.д.) -->
    <mvc:annotation-driven/>

    <!-- Сканирование пакета контроллеров -->
    <context:component-scan base-package="ru.forum.web.controller"/>

    <!-- Резолвер JSP-представлений -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
        <property name="order" value="1"/>
    </bean>

    <!-- Перехватчик для защиты страниц, требующих авторизации -->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/topic/new/**"/>
            <mvc:mapping path="/post/**"/>
            <mvc:mapping path="/user/cabinet"/>
            <mvc:mapping path="/user/cabinet/**"/>
            <bean class="ru.forum.web.interceptor.AuthInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- Статические ресурсы (CSS, JS и т.д.) -->
    <mvc:resources mapping="/resources/**" location="/resources/"/>

</beans>
